# VIZME login theme — extends Keycloak default (PatternFly + shared scripts).
# Child theme must repeat `styles` if set, or parent login.css is not linked.
parent=keycloak

styles=css/login.css css/vizme.css

# Optional: avoid branding the card header with keycloak.org
kcLogoLink=
